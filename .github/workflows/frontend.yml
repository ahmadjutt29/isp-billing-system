name: Frontend CI/CD (Local Deployment)

on:
  push:
    branches: [develop, main, master]
    paths:
      - 'frontend/frontend/**'
      - '.github/workflows/frontend-ci-cd.yml'
  pull_request:
    branches: [develop, main, master]
    paths:
      - 'frontend/frontend/**'
      - '.github/workflows/frontend-ci-cd.yml'

env:
  NODE_VERSION: '20'
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}/frontend

jobs:
  ci:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install dependencies
        working-directory: ./frontend/frontend
        run: npm ci

      - name: Run Linter
        working-directory: ./frontend/frontend
        run: npm run lint
        continue-on-error: true

      - name: Type Check
        working-directory: ./frontend/frontend
        run: npx tsc --noEmit

      - name: Build
        working-directory: ./frontend/frontend
        run: npm run build

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: frontend/frontend/dist
          retention-days: 7

  docker-build:
    name: Build Docker Image (Optional)
    runs-on: ubuntu-latest
    needs: ci
    if: github.event_name == 'push'
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=sha,prefix=
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build & Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend/frontend
          file: ./frontend/frontend/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy-staging:
    name: Deploy to Local Staging
    runs-on: ubuntu-latest
    needs: docker-build
    if: github.ref == 'refs/heads/develop'

    steps:
      - uses: actions/checkout@v4

      - name: Download Build Artifact
        uses: actions/download-artifact@v4
        with:
          name: frontend-build
          path: ./frontend-build

      - name: Deploy to Local Staging
        run: |
          mkdir -p ~/react-app/staging/backups
          TIMESTAMP=$(date +%Y%m%d%H%M%S)
          if [ -d ~/react-app/staging/current ]; then
            mv ~/react-app/staging/current ~/react-app/staging/backups/$TIMESTAMP
          fi
          cp -r ./frontend-build ~/react-app/staging/current
          echo "✅ Staging deployment complete. Run 'serve ~/react-app/staging/current -l 8081' to test."

  deploy-production:
    name: Deploy to Local Production
    runs-on: ubuntu-latest
    needs: docker-build
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'

    steps:
      - uses: actions/checkout@v4

      - name: Download Build Artifact
        uses: actions/download-artifact@v4
        with:
          name: frontend-build
          path: ./frontend-build

      - name: Deploy to Local Production
        run: |
          mkdir -p ~/react-app/production/backups
          TIMESTAMP=$(date +%Y%m%d%H%M%S)
          if [ -d ~/react-app/production/current ]; then
            mv ~/react-app/production/current ~/react-app/production/backups/$TIMESTAMP
          fi
          cp -r ./frontend-build ~/react-app/production/current
          echo "✅ Production deployment complete. Run 'serve ~/react-app/production/current -l 8082' to test."
